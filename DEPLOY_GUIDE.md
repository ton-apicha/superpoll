# 🚀 วิธีนำ SuperPoll ขึ้นออนไลน์ (Public)

## วิธีที่ 1: Streamlit Community Cloud (แนะนำ! ฟรี, ถาวร)
วิธีนี้ดีที่สุด สเถียร และไม่ต้องเปิดคอมทิ้งไว้

### 1. นำ Code ขึ้น GitHub
1. สมัคร [GitHub](https://github.com/) (ถ้ายังไม่มี)
2. สร้าง Repository ใหม่ (ตั้งชื่อเช่น `superpoll`)
3. อัพโหลดไฟล์ทั้งหมดในโฟลเดอร์นี้ขึ้นไป (ยกเว้น `venv` และ `__pycache__`)
   - หรือใช้ Git Command:
     ```bash
     git init
     git add .
     git commit -m "First commit"
     git branch -M main
     git remote add origin https://github.com/USERNAME/superpoll.git
     git push -u origin main
     ```

### 2. Connect กับ Streamlit Cloud
1. ไปที่ [share.streamlit.io](https://share.streamlit.io/)
2. Login ด้วย GitHub
3. กด **"New app"**
4. เลือก Repository `superpoll` ของคุณ
5. ช่อง **Main file path** ใส่ `app.py`
6. กด **Deploy!**

รอสักพัก เว็บของคุณจะออนไลน์ที่ `https://superpoll-xxxx.streamlit.app` ส่งลิ้งค์ให้ใครก็ได้เลย!

---

## วิธีที่ 2: Ngrok (ชั่วคราว, เร็ว, รันจากเครื่องเรา)
วิธีนี้เหมาะกับการเทสเร็วๆ โดยไม่ต้องอัพขึ้น Git แต่ *ถ้าปิดคอม หรือปิด Terminal เว็บจะเข้าไม่ได้*

### 1. ติดตั้ง Ngrok
1. ไปที่ [ngrok.com](https://ngrok.com/) สมัครสมาชิก
2. Download และติดตั้งตามคู่มือ
3. รันคำสั่งนี้ใน Terminal (หลังจากรัน Streamlit แล้ว):
   ```bash
   ngrok http 8501
   ```
4. Ngrok จะให้ URL มา (เช่น `https://random-name.ngrok-free.app`) ก็นำไปแจกได้เลย

---

## ⚠️ ข้อควรระวัง
- **Database (SQLite)**: 
  - **Streamlit Cloud**: ข้อมูลจะหายถ้า App ปิดหรือ Restart (เพราะ SQLite เป็นไฟล์ Local). แนะนำให้เปลี่ยนไปใช้ **Google Sheets** หรือ **PostgreSQL** (เช่น Neon.tech ฟรี) ถ้าต้องการเก็บข้อมูลถาวรจริงๆ
  - **Ngrok**: ข้อมูลยังอยู่ตลอดเพราะรันบนเครื่องเรา

## ✅ ไฟล์ที่เตรียมให้แล้ว
- `requirements.txt`: รายชื่อ Library ทั้งหมดที่ต้องใช้ (Streamlit Cloud ต้องใช้ไฟล์นี้เพื่อลงโปรแกรม)
